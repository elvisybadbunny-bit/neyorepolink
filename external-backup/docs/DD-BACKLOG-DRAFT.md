# DD — Founder's New Backlog (2026-07-14), Draft for Confirmation

Founder's real message, broken into discrete, numbered items so nothing gets lost or misbuilt. Each item below needs to be confirmed/clarified before building (see questions at the bottom).

## Student Import (`/students/import`)

**DD.1 — Compulsory subjects as buttons, not typed text.**
Currently a school types compulsory subject names/codes into a free-text field before importing. Replace with clickable subject buttons — showing the real CBE subject list when importing into a CBE class, and the real 8-4-4 subject list when importing into an 8-4-4 class. A school clicks to select, never types (avoids typos/misspellings). "After one confirms the others get removed after import is done too" — needs clarification: does this mean (a) the selection resets after the import completes (so the next import starts fresh), or (b) once a subject is clicked/selected as compulsory it's removed from the remaining clickable list (so it can't be double-added)?

**DD.2 — Custom field bug: a school can't actually add their own custom field.**
Confirmed live via direct testing: selecting "Custom field" on a column and typing a label triggers a real `422 "Please check the form."` error and the import preview breaks. This is a real, reproduced bug — needs a real fix.

**DD.3 — Import should read ALL subjects in a student's row and avoid duplicating compulsory subjects.**
If a school's own file already lists a compulsory subject (e.g. English) inside that row's own Subjects column, and English is also declared compulsory for the whole run, NEYO should not create two separate subject-selection entries for it — it should recognize the overlap and keep exactly one.

## Combination Engine

**DD.4 — Auto-group students into elective combinations from up to 3 electives declared in the import.**
When a student's row lists their elective choices (up to 3), the system should group students into real combinations from those choices (not just from a manual portal). If a Mathematics variant (e.g. Core vs Essential) is explicitly mentioned in the row, use it. If not mentioned, the system should check the student's own declared pathway (STEM vs the other CBE pathways) and auto-assign the correct required Maths variant for that pathway. Compulsory subjects (done by everyone) are excluded from this combination logic entirely.

**DD.5 — Combination print should be A4 and printable.**
The existing subject-combination roster print (from BB.7) should render cleanly on A4 for physical printing.

## Confirm → Auto-Allocate Flow

**DD.6 — Once students confirm their combination is correct, the system may proceed to allocate classes AND assign teachers automatically.**
After a human confirms a student's combination is correct, NEYO should be allowed to (a) allocate the student to a class (this already exists via BB.4's "Allocate Class" flow) and (b) auto-assign teachers to the resulting subject/class combinations, respecting a real minimum-number-of-lessons-per-teacher fairness rule (similar to the existing fair-allocation logic already used elsewhere, e.g. AA.9's `autoAssignTeachersToClasses()`).

**DD.7 — After that analysis, combinations should appear automatically in the "Combination" tab, and electives automatically in the "Electives" tab — no manual re-entry.**
Needs clarification on which exact screens "Combination tab" and "Electives tab" refer to (likely inside Academics / Smart Timetable, but needs confirming).

## Smart Timetable / Lesson Requirements Configuration

**DD.8 — Lesson-requirement counts (compulsory, elective, or other) should show inside the "doubles and singles" configuration tab, so a school can set them there.**

**DD.9 — Class-subject/lesson-requirement configuration should show per GRADE as a whole, not per individual stream.**
E.g. "Grade 10 Lesson Requirements" as one combined config screen, not "Grade 10 East", "Grade 10 West" etc. rendered and configured separately — since all streams of a grade normally study the same amount. A school should still be able to open a SPECIFIC stream to configure it differently, but the default/normal view should be the whole grade combined into one input.

**DD.10 — Same grouping logic should apply to time-slot/period configuration.**
If the whole school uses one method, render/configure it once for the whole school. If a specific group of grades shares the same config (e.g. Grade 1–3), render/configure that group once rather than repeating the same setup for every individual class/stream.

## Prints

**DD.11 — A new print showing teacher allocation and the subjects each teacher covers.**

## Cross-Cutting Bugs

**DD.12 — Generic "Please check the form." error appears on several pages when saving, hiding the real problem.**
Root cause confirmed: `src/lib/api/respond.ts`'s `handleError()` catches every `ZodError` and always returns the same generic message ("Please check the form.") — the real per-field reasons ARE included in the response's `error.fields` object, but many pages' UI never reads or displays them, just shows the generic toast. Needs a real, systematic fix so the ACTUAL field-level problem is shown to the school, not just a generic message.

**DD.13 — Double-period lessons should render as one merged cell.**
If a lesson is scheduled as a genuine double (two consecutive periods, e.g. Period 1 and Period 2, same subject/class/teacher), the timetable display should merge them into a single visual cell instead of showing two identical adjacent cells.

**DD.14 — Teacher allocation should always be driven by each teacher's own real `TeacherSubject` links.**
This principle was already built for AA.9's scope fix and class-teacher assignment, but the founder is flagging it again — needs investigation into whether there's a remaining screen/flow where teacher-allocation dropdowns are NOT filtered by a teacher's own real declared subjects.

---

## Open Questions Before Building (see `ask_user`) — RESOLVED 2026-07-14
1. DD.1 — confirmed: BOTH behaviors. A subject button disappears from the remaining pick-list the moment it's selected as compulsory (can't be double-added), AND the whole selection resets once an import run completes (next import starts fresh).
2. DD.7 — confirmed: "the Combination tab" and "the Electives tab" are screens inside **Smart Timetable** (not the existing Academics-section Combination Groups/Elective Block manual-build screens — those may need to gain a "generated automatically, no manual input needed" real read-only/auto-populated state instead, to be confirmed further when DD.4-DD.7 are actually built).
3. Order of work confirmed: fix the 2 already-reproduced concrete bugs first (DD.2 custom-field-in-import, DD.12 generic "Please check the form" error), then Import improvements (DD.1, DD.3), then Combination engine (DD.4-DD.7), then Smart Timetable UI regrouping (DD.8-DD.10), then prints (DD.5, DD.11), then double-period rendering (DD.13) and the TeacherSubject audit (DD.14).

**Status**: DD.2 and DD.12 (the confirmed bugs) are FIXED, tested, committed (`ea6d8b4`), and pushed. DD.1 (compulsory-subject buttons) is FIXED, tested, committed (`3d4ddf8`), and pushed. DD.3 (avoid duplicating a subject already listed compulsorily) was investigated and found to ALREADY work correctly — confirmed via a real live test, no code change needed. DD.4 part 1 (pathway-aware Core/Essential Mathematics auto-selection during import, with the real official STEM-always-Core exception rule confirmed via research) is FIXED, tested, committed (`89ff151`), and pushed. DD.5 (confirmed the pre-existing combination roster is already A4-printable, no code change needed) and DD.11 (new per-subject roster print with each student's real class) are FIXED, tested, committed (`596ef1c`), and pushed. DD.6 (auto-allocate class+teacher after a combination is confirmed) and DD.7 (confirmed combinations/electives auto-appear in their real tabs, zero manual re-entry) are FIXED, tested, committed (`53f9289`), and pushed — this also fixed a real, deeper pre-existing gap in `runAutoGroupingPreview()` that silently ignored genuinely classless students even when a level's classes already existed.

**DD.8/DD.9 — DONE (backend `66e814f`, frontend `97d6fa1`), both pushed.** DD.8 ("doubles and singles" lesson-requirement counts should be settable in the Smart Timetable tab) was already true structurally (the existing "Class subject lesson requirements" card already lived in Smart Timetable) — the real remaining gap was DD.9's own request (per-grade, not per-stream). Both are now solved together: the "Class subject lesson requirements" card groups classes by real grade/level (e.g. "Grade 10 (3 streams)") as collapsible cards. For each subject, a real agreement check (`getClassSubjectNeedAgreementForLevel`/client-side `needAgreementForLevelSubject`) decides whether every real stream currently agrees on `lessonsPerWeek, doubleCount, allowSplitDouble, venueId, requiresMovement, noLabAccess, labPriority, rotateTeacherEachTerm` (deliberately excluding `teacherId` — a school may legitimately want a different teacher per stream for the same subject). When they agree (or none have a value yet), ONE shared row is shown and saved to every real stream at once via `saveNeedForLevel()` / `save_need_for_level`. When streams genuinely disagree, each stream gets its own row with an amber "Streams differ" badge — a real customised stream can never be silently overwritten. A school can also explicitly open "Set a specific stream differently" to edit one stream even when the grade currently agrees. Two real Rules-of-Hooks bugs were found and fixed during this exact build (a `useMemo` originally placed after the component's early return, and a leftover duplicate declaration) — both confirmed fixed via `tsc --noEmit` (0 errors) and a live Playwright console-log capture (zero "change in the order of Hooks" / "Cannot update a component" errors), plus a full regression sweep (dd9-dd10 test 17/17, aa4/aa8/aa9/cc1/l7 all green).

**DD.10 — Schedule Rules dialog DONE (`89c75b6`, pushed), cross-grade/whole-school grouping still open.** The "Schedule Rules" dialog (`ClassConfigModal`, opened from the Timetable tab's "Schedule rules" button) now has the same real whole-grade-agreement treatment as the Lesson Requirements card: when the class being configured belongs to a grade with more than one real stream, a real agreement check (`getTimetableConfigAgreementForLevel`) runs and the dialog defaults to whole-grade scope (Save writes identically to every real stream via `saveTimetableConfigForLevel`/`save_config_for_level`) only when every real stream already agrees or none have a config row yet; if streams genuinely disagree, the dialog defaults to editing only the one stream that was opened, with an amber "Streams differ" notice, and a school can always explicitly switch between "edit the whole grade" and "edit only this stream". Confirmed live via Playwright against Uwezo Primary & Junior School's real 27-class/9-grade/3-stream-per-grade data: the dialog correctly showed the amber "Streams differ — editing only Grade 1 Amani" state for a grade with genuine per-stream differences, with zero React Hooks-order console errors. Confirmed via `tsc --noEmit` (0 errors) and a full regression sweep re-run (dd9-dd10 test 17/17, aa4/aa8/aa9/cc1/l7 all green, unaffected by this change).

**DD.10 continued — REAL BUG FIX (`0909bd1`, pushed).** The founder's own follow-up ("if different grades or classes have different systems in how their time slots look like ... their teachers maybe different and maybe time slots are different, the engine should be able to allow that and make a timetable that doesn't teacher class too in that case") led to investigating and confirming a genuine, serious PRE-EXISTING bug in the real, currently-used Master Button engine (`timetable-engine.service.ts`'s `startGeneration`/`runGeneration`/`buildAndSolve`): its own teacher-conflict check keyed purely on `${teacherId}:${day}:${period}` — comparing raw PERIOD NUMBERS, never real wall-clock time. Two real classes with genuinely different period lengths (e.g. 30-minute periods vs 40-minute periods, as in the founder's own pre-primary/primary/junior/senior scenario) can have the exact same period NUMBER land at completely different real times — so a shared teacher COULD be double-booked in actual wall-clock time while the solver saw zero conflict. Reproduced live via a real adversarial test before the fix, showing a genuine 10-minute real overlap. FIXED with new `realPeriodStartMinutes()`/`realPeriodEndMinutes()`/`realPeriodsOverlap()` helpers (mirroring the founder-facing UI's own time-math formula) plus a new `teacherBookingsByTeacher` index checked via `teacherHasRealTimeConflict()` — wired into the main backtracking solver's `periodFree()` and the Options/Elective Block placement loop. Confirmed via `scripts/dd10-cross-level-teacher-conflict-test.ts` (4/4, run via the REAL production `startGeneration()` path) and a full regression sweep, all green.

The founder's own further request for cross-grade/whole-school grouping beyond single-grade ("if the whole school got the same method it renders whole school" and "if a group has a certain same eg grade 1-3 it renders that to avoid repeating") is STILL OPEN and has NOT yet been designed or built — needs its own `ask_user` clarification pass (e.g. auto-detected agreement across contiguous grades vs. a school-defined named group) before being built.

**DD.13 — DONE (`2e8132c` live in-app, `6148ba2` print), both pushed.** "If a lesson is scheduled as a genuine double (two consecutive periods, e.g. Period 1 and Period 2, same subject/class/teacher), the timetable display should merge them into a single visual cell instead of showing two identical adjacent cells." New `computeDoubleSpanSecondHalves()` (live UI) / `computeDoubleSpanSecondHalvesForPrint()` (print route) detect genuine doubles directly from the real persisted `TimetableSlot` data (same class/subject/teacher/slotType, consecutive periods, same day) — never a separate stored flag. Both the live Timetable tab (horizontal + vertical layouts, via `rowSpan`/`colSpan`) and the dedicated `/print/timetable` route (single-class, single-teacher, and bulk printing) now merge these into one cell with a real "Double"/"DOUBLE" badge. ELECTIVE_BLOCK cells are deliberately excluded (they already have their own distinct rendering). Confirmed via `scripts/dd13-double-period-merge-test.ts` (7/7, run twice, driven by a REAL Master Button generation with a real `doubleCount=1` subject) plus a full regression sweep, all green.

**DD.14 — DONE (`2aac16b`), pushed.** Audit request: "teacher allocation should always be driven by each teacher's own real `TeacherSubject` links." Found and fixed a real, systemic gap: the backend's own `getTimetableInputs()` has ALWAYS returned the school's real `TeacherSubject` links as `teacherAssoc`, but the frontend never actually captured it into any state anywhere in `academics-client.tsx` — so five real subject-specific teacher pickers (Combination classes, Options/Elective Block builder, the manual single-slot editor, Auto-fill week, Bulk Saturday Scheduler) all showed EVERY real teacher regardless of subject qualification. Fixed by capturing `teacherAssoc` in both `TimetableEngineTab` and `TimetableTab` and adding a `teachersQualifiedFor()`/`staffQualifiedFor()` filter helper (falls back to the full staff list only when a subject genuinely has no qualified teacher linked yet, so a school is never shown a dead empty dropdown). Confirmed ALREADY correct and left untouched: `promotion-client.tsx`'s teacher-allocation-review recommendations (already using the real `recommendTeacherForSubject()`), the HOD/Teacher-Time-Off pickers (correctly not subject-specific), and the substitute-reassignment picker (a deliberate, documented human-override design). `SubjectNeedsModal` was found to be genuinely dead code (defined but never rendered) — left untouched. Confirmed via `scripts/dd14-teacher-subject-filter-audit-test.ts` (7/7, run twice) plus a full regression sweep, all green.

Next: clarify and build DD.10's remaining cross-grade/whole-school grouping scope (the only open item left in the DD.1-DD.14 backlog), then move to AA.6 (the one remaining original Part AA backlog item).
