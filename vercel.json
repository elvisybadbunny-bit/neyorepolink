{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["fra1"],
  "buildCommand": "node scripts/auto-resolve-failed-migrations.js && prisma generate && prisma migrate deploy && next build",
  "crons": [
    {
      "path": "/api/jobs/tick",
      "schedule": "0 3 * * *"
    }
  ]
}
