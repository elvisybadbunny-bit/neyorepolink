import{NextRequest}from"next/server";import{requirePermission}from"@/lib/core/session";import{handleError,ok}from"@/lib/api/respond";import{closePracticeTeacherPlacement,practiceTeacherBoard,savePracticeTeacherPlacement}from"@/lib/services/practice-teacher.service";export const dynamic="force-dynamic";export async function GET(){try{const u=await requirePermission("academics.view");return ok({board:await practiceTeacherBoard(u)})}catch(e){return handleError(e)}}export async function POST(req:NextRequest){try{const u=await requirePermission("academics.manage");const b=await req.json();if(b.action==="save")return ok({placement:await savePracticeTeacherPlacement(u,b)});if(b.action==="close")return ok({placement:await closePracticeTeacherPlacement(u,b.id,b.status)});throw new Error("Unsupported practice-teacher action.")}catch(e){return handleError(e)}}
