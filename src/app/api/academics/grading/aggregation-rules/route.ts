import{NextRequest}from"next/server";import{requirePermission}from"@/lib/core/session";import{handleError,ok}from"@/lib/api/respond";import{reportComputationSetup,saveReportComputationRule}from"@/lib/services/report-computation-rule.service";export const dynamic="force-dynamic";export async function GET(req:NextRequest){try{const u=await requirePermission("academics.view");return ok(await reportComputationSetup(u,req.nextUrl.searchParams.get("termId")||""))}catch(e){return handleError(e)}}export async function POST(req:NextRequest){try{const u=await requirePermission("academics.manage");return ok({rule:await saveReportComputationRule(u,await req.json())})}catch(e){return handleError(e)}}
