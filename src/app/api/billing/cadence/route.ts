import{NextRequest}from"next/server";import{requirePermission}from"@/lib/core/session";import{handleError,ok}from"@/lib/api/respond";import{cycleAmount,setTenantBillingCycle}from"@/lib/services/billing-cycle.service";import{db}from"@/lib/db";export const dynamic="force-dynamic";export async function GET(){try{const u=await requirePermission("owner.dashboard");const s=await db.subscription.findUniqueOrThrow({where:{tenantId:u.tenantId}});return ok({billingCycle:s.billingCycle,amountKes:cycleAmount(s.sizeBasedPriceKes||s.grandfatheredPrice,s.pricingMode,s.billingCycle as any),pricingMode:s.pricingMode})}catch(e){return handleError(e)}}export async function POST(req:NextRequest){try{const u=await requirePermission("owner.dashboard");const b=await req.json();return ok(await setTenantBillingCycle(u,b.billingCycle))}catch(e){return handleError(e)}}
