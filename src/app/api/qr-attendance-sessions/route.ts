import{NextRequest}from"next/server";import{requirePermission}from"@/lib/core/session";import{handleError,ok}from"@/lib/api/respond";import{closeQrAttendanceSession,createQrAttendanceSession,listQrAttendanceSessions}from"@/lib/services/qr-attendance-session.service";export const dynamic="force-dynamic";export async function GET(){try{const u=await requirePermission("attendance.view");return ok({sessions:await listQrAttendanceSessions(u)})}catch(e){return handleError(e)}}export async function POST(req:NextRequest){try{const u=await requirePermission("attendance.record");const b=await req.json();if(b.action==="create")return ok({session:await createQrAttendanceSession(u,b)});if(b.action==="close")return ok({session:await closeQrAttendanceSession(u,b.id)});throw new Error("Unsupported QR attendance session action.")}catch(e){return handleError(e)}}
