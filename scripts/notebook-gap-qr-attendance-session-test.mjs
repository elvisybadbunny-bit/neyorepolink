import{readFileSync}from"node:fs";let n=0;const c=(x,m)=>{if(!x)throw new Error(`FAIL: ${m}`);console.log(`PASS ${++n}: ${m}`)};const schema=readFileSync("prisma/schema.prisma","utf8"),svc=readFileSync("src/lib/services/qr-attendance-session.service.ts","utf8"),scan=readFileSync("src/lib/services/qr-scan.service.ts","utf8"),route=readFileSync("src/app/api/qr-scan/attendance/route.ts","utf8"),ui=readFileSync("src/components/security/qr-scan-station.tsx","utf8");c(schema.includes("model QrAttendanceSession")&&schema.includes("model QrAttendanceResponse"),"unique attendance sessions and responses are persisted");c(schema.includes("@@unique([sessionId, studentId])"),"database enforces one accepted response per learner per session");c(svc.includes("randomBytes(12)")&&svc.includes("expiresAt"),"each session receives a cryptographically random expiring code");c(scan.includes("status: \"OPEN\"")&&scan.includes("expiresAt: { gt: new Date() }")&&scan.includes("session.classId !== student.classId"),"scan validates open session, expiry and optional class scope");c(scan.includes("This learner has already responded to this attendance session"),"repeat response is rejected clearly");c(route.includes("sessionId")&&route.includes("scanForAttendance(user, scanned, status, sessionId)"),"attendance scan API carries selected session into the service");c(ui.includes("Unique attendance session")&&ui.includes("Open 60-minute session")&&ui.includes("sessionId: attendanceSessionId")&&ui.includes("sessionClassId"),"QR checkpoint has reachable class/school session creation and selection");c(ui.includes("QRCode.toDataURL")&&ui.includes("Session QR"),"each session has a distinct printable/displayable QR");console.log(`NOTEBOOK GAP BATCH 4 COMPLETE: ${n}/8`);
