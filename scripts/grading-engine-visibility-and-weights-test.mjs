import{readFileSync}from"node:fs";let n=0;const c=(x,m)=>{if(!x)throw new Error(`FAIL: ${m}`);console.log(`PASS ${++n}: ${m}`)};const academics=readFileSync("src/components/academics/academics-client.tsx","utf8"),nav=readFileSync("src/lib/core/navigation.ts","utf8"),ui=readFileSync("src/components/academics/subject-paper-grading-card.tsx","utf8"),route=readFileSync("src/app/api/academics/grading/papers/route.ts","utf8"),grading=readFileSync("src/lib/services/grading-engine.service.ts","utf8"),compute=readFileSync("src/lib/services/computation-engine.service.ts","utf8");c(academics.includes('{ key: "computation" as const, label: "Grading Engine"')&&!academics.includes('isCurriculumEngineEnabled ? [\n      { key: "computation"'),"Grading Engine is visible for every academic system, not only curriculum-engine schools");c(nav.includes('/academics?tab=grading'),"main School OS navigation links directly to Grading Engine");c(ui.includes("Subject-specific papers, marks out of and weights")&&ui.includes("MarksGridClient"),"reachable UI configures papers and opens the paper-aware marks grid");c(route.includes("configureSubjectPapers")&&route.includes('requirePermission("academics.manage")'),"paper configuration is full-stack and leadership protected");c(grading.includes("Paper weights must sum to exactly 100%")&&grading.includes("outOfMarks"),"each subject/class paper has a validated maximum and weights total 100");c(grading.includes("outOfMarks: exam.maxMarks"),"unconfigured subject defaults to that exam's actual maximum instead of always 100");c(compute.includes("(finalScore / 100) * exam.maxMarks"),"weighted paper result is stored on the exam's declared final scale");c(compute.includes("normalizedMark")&&compute.includes("sourceExam.maxMarks"),"term aggregation normalizes exams with different maximum marks before weighting");console.log(`GRADING ENGINE VISIBILITY AND WEIGHTS COMPLETE: ${n}/8`);
